import { ScreenStub } from "@/components/ScreenStub";

export default function RecordScreen() {
  return (
    <ScreenStub
      title="Record"
      description="Capture a meeting on your phone. EchoBrief uploads it and returns a summary, action items, and a searchable transcript."
    />
  );
}
