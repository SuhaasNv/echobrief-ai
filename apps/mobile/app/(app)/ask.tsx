import { ScreenStub } from "@/components/ScreenStub";

export default function AskScreen() {
  return (
    <ScreenStub
      title="Ask"
      description="Ask a question and get an answer drawn from every meeting you've recorded, with sources."
    />
  );
}
