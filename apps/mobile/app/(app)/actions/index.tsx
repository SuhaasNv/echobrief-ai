import { ScreenStub } from "@/components/screen-stub";

export default function ActionsScreen() {
  return (
    <ScreenStub description="Every commitment, owner, and due date EchoBrief pulled out of your meetings, in one checkable list." />
  );
}
