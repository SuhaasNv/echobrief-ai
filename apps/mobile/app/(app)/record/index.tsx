import { ScreenStub } from "@/components/screen-stub";

export default function RecordScreen() {
  return (
    <ScreenStub description="Capture a meeting on your phone. EchoBrief uploads it and returns a summary, action items, and a searchable transcript." />
  );
}
