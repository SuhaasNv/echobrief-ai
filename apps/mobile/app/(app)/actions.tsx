import { ScreenStub } from "@/components/ScreenStub";

export default function ActionsScreen() {
  return (
    <ScreenStub
      title="Actions"
      description="Every commitment, owner, and due date EchoBrief pulled out of your meetings, in one checkable list."
    />
  );
}
