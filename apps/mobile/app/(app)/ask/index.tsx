import { ScreenStub } from "@/components/screen-stub";

export default function AskScreen() {
  return (
    <ScreenStub description="Ask a question and get an answer drawn from every meeting you've recorded, with sources." />
  );
}
