# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Environment
.env
.env.local
.env.*.local
dist-api/

# Test coverage reports — regenerable with `npm run test:coverage`
coverage/

# Database backups — contain PII (emails, password hashes). Never commit.
backups/

# AI Agent Skills — locally installed, regenerable with `npx skills add`
.agents/
.claude/skills/
.cortex/skills/
skills-lock.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Claude Code settings. Has held a live API key; never commit.
.claude/settings.local.json

# Python bytecode from docs/brand/build-icons.py
__pycache__/

# Generated test recordings (scripts/make-test-audio.sh) — regenerate, never commit
test-audio/

# Transient design-review screenshots
.judge-shots/
