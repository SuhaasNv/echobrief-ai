---
created: 2026-05-20T02:13:53.498Z
session: d0923320-76ab-411d-b7d3-14ff14d591eb
working_directory: /Users/suhaasnv/Documents/Desktop ALT/portfolioprojects/echobrief-ai
---

# Test Coverage Expansion Plan

## Context

**Current State:**
- **Vitest** configured with path aliases (`@`), sequential test runs, 30s timeout
- **4 integration tests** exist: `auth.test.ts`, `health.test.ts`, `meetings.test.ts`, `admin.test.ts`
- **2 unit tests** exist: `r2.test.ts` (service), `chunking.test.ts` (lib)
- **Test status**: 27 passing, 3 failing (auth signup/login tests likely due to subscription backfill)
- **Coverage thresholds**: 60% statements, 50% branches/functions, 60% lines (aspirational)
- **New code added**: Comprehensive subscription system (18 files) with **zero test coverage**

**Key Findings:**
1. **Subscription system completely untested**: `usage-tracker.ts`, `subscription.ts` route, quota middleware, feature gates
2. **No frontend/React component tests**: Only backend integration tests exist
3. **Limited service-layer unit tests**: Only 2 service unit tests (r2, chunking)
4. **No middleware tests**: auth, quota, rate-limit, workspace middleware untested
5. **No utility tests**: date-utils, features.ts, schemas untested

**Existing Patterns:**
- Unit tests: `src/server/services/__tests__/service-name.test.ts`
- Integration tests: `tests/integration/feature.test.ts`
- Vitest + `describe/it/expect` syntax
- Test helpers: `postJson()`, `get()`, `uniqueEmail()` for integration tests
- Database cleanup in `afterAll()` hooks

## Implementation Steps

### **Phase 1: Unit Tests for Subscription System (Priority: HIGH)**

**1.1 Test `usage-tracker.ts` service** (`src/server/services/__tests__/usage-tracker.test.ts`)
- **Functions to test**:
  - `logTranscription()` - Insert usage_logs row with minutes
  - `logAIQuery()` - Insert query usage
  - `logFlashcardGeneration()` - Insert flashcard count
  - `getCurrentUsage()` - Aggregate monthly usage by YYYY-MM period
  - `getUserTier()` - Fetch tier from subscriptions table
  - `checkQuota()` - Enforce limits for free tier, allow unlimited for paid
  - `checkWorkspaceQuota()` - Enforce workspace count limits
- **Test cases**:
  - ✓ Log operations create correct usage_log rows
  - ✓ getCurrentUsage aggregates correctly across months
  - ✓ checkQuota denies free tier at limits (300 min, 10 queries, 3 flashcards)
  - ✓ checkQuota allows unlimited for student/pro/team
  - ✓ checkWorkspaceQuota denies free tier at 1 workspace
  - ✓ getCurrentUsage handles empty usage (returns zeros)
  - ✓ Period rollup works (YYYY-MM format)

**1.2 Test `features.ts` utilities** (`src/lib/__tests__/features.test.ts`)
- **Functions to test**:
  - `hasFeature()` - Check boolean features by tier
  - `TIER_FEATURES` - Validate configuration completeness
  - `TIER_PRICING` - Validate pricing structure
- **Test cases**:
  - ✓ hasFeature() returns correct boolean for each tier
  - ✓ Free tier has expected restrictions
  - ✓ Student tier has flashcards, no integrations
  - ✓ Pro tier has integrations + email generation
  - ✓ Team tier has unlimited history + shared workspaces
  - ✓ All tiers defined in TIER_FEATURES
  - ✓ TIER_PRICING excludes 'free' tier correctly

**1.3 Test `date-utils.ts` utilities** (`src/lib/__tests__/date-utils.test.ts`)
- **Functions to test**: All 9 functions (formatDate, formatDuration, formatTimestamp, formatUptime, formatDue, isOverdue, formatDateTime, getTodayFormatted)
- **Test cases**:
  - ✓ formatDate handles ISO strings, includeYear flag
  - ✓ formatDuration converts seconds to M:SS
  - ✓ formatTimestamp pads zeros correctly
  - ✓ formatUptime shows days/hours/minutes
  - ✓ isOverdue detects past dates
  - ✓ Edge cases: null inputs, invalid dates, boundary values

### **Phase 2: Integration Tests for Subscription API (Priority: HIGH)**

**2.1 Test `/api/v1/subscription` route** (`tests/integration/subscription.test.ts`)
- **Endpoints to test**:
  - GET `/subscription` - Current tier + usage + limits
  - POST `/subscription/upgrade` - Stripe upgrade placeholder
  - POST `/subscription/cancel` - Cancel subscription placeholder
- **Test cases**:
  - ✓ GET returns free tier for new user
  - ✓ GET includes usage aggregates (transcription_minutes, ai_queries, flashcards)
  - ✓ GET returns correct limits based on tier
  - ✓ GET includes feature flags (integrations, email_generation, etc.)
  - ✓ GET workspace_count matches actual count
  - ✓ POST /upgrade validates tier enum (student/pro/team)
  - ✓ POST /upgrade validates billing_interval (monthly/annual)
  - ✓ POST /upgrade returns placeholder message (no Stripe yet)
  - ✓ POST /cancel returns placeholder message
  - ✓ 401 when not authenticated

**2.2 Test quota middleware** (`tests/integration/quota-middleware.test.ts`)
- **Middleware to test**: `requireTranscriptionQuota`, `requireAIQueryQuota`, `requireFlashcardQuota`
- **Test cases**:
  - ✓ Transcription blocked when free tier at 300 min
  - ✓ AI query blocked when free tier at 10 queries
  - ✓ Flashcard blocked when free tier at 3 flashcards
  - ✓ 429 response includes usage + limit in body
  - ✓ Paid tiers bypass quota checks
  - ✓ Usage logs correctly after quota check

### **Phase 3: Integration Tests for Existing Routes (Priority: MEDIUM)**

**3.1 Test `/api/v1/workspaces` route** (`tests/integration/workspaces.test.ts`)
- **Currently untested** - critical for quota enforcement
- **Test cases**:
  - ✓ POST /workspaces denied when free tier at 1 workspace
  - ✓ POST /workspaces succeeds for paid tiers
  - ✓ GET /workspaces returns all user workspaces
  - ✓ PATCH /workspaces/:id updates name/color/kind
  - ✓ DELETE /workspaces/:id removes workspace

**3.2 Test `/api/v1/flashcards` route** (`tests/integration/flashcards.test.ts`)
- **Currently untested**
- **Test cases**:
  - ✓ POST /flashcards denied for free tier (feature gate)
  - ✓ POST /flashcards succeeds for student+ tiers
  - ✓ GET /flashcards/:meetingId returns flashcards
  - ✓ DELETE /flashcards/:id removes flashcard

**3.3 Test `/api/v1/integrations` route** (`tests/integration/integrations.test.ts`)
- **Currently untested**
- **Test cases**:
  - ✓ POST /integrations denied for free/student tiers
  - ✓ POST /integrations succeeds for pro+ tiers
  - ✓ GET /integrations returns connected integrations
  - ✓ DELETE /integrations/:id disconnects

### **Phase 4: Unit Tests for Middleware (Priority: MEDIUM)**

**4.1 Test auth middleware** (`src/server/api/middleware/__tests__/auth.test.ts`)
- **Functions to test**: `requireAuth`, JWT verification
- **Test cases**:
  - ✓ requireAuth extracts user from valid JWT
  - ✓ requireAuth returns 401 for missing header
  - ✓ requireAuth returns 401 for malformed token
  - ✓ requireAuth returns 401 for expired token

**4.2 Test workspace middleware** (`src/server/api/middleware/__tests__/workspace.test.ts`)
- **Functions to test**: `requireWorkspace`, workspace ownership
- **Test cases**:
  - ✓ Extracts workspaceId from header
  - ✓ Validates user ownership
  - ✓ Returns 403 when user not owner/member
  - ✓ Falls back to default workspace if header missing

**4.3 Test rate-limit middleware** (`src/server/api/middleware/__tests__/rate-limit.test.ts`)
- **Functions to test**: Rate limiting logic
- **Test cases**:
  - ✓ Allows requests under limit
  - ✓ Returns 429 when rate exceeded
  - ✓ Rate window resets after time

### **Phase 5: Unit Tests for Services (Priority: LOW)**

**5.1 Test `openai.ts` service** (`src/server/services/__tests__/openai.test.ts`)
- Mock OpenAI API responses
- Test prompt construction, error handling

**5.2 Test `resend.ts` service** (`src/server/services/__tests__/resend.test.ts`)
- Mock email sending
- Test email template rendering

### **Phase 6: Frontend Component Tests (Priority: LOW - Future)**

**Would require adding @testing-library/react:**
- Test `<UpgradeModal />` rendering with different tiers
- Test `<QuotaBanner />` shows correct warnings
- Test workspace creation button with quota enforcement
- Test hooks: `useSubscription()`, `useQuotaStatus()`

## Verification

**After each phase:**
1. **Run tests**: `npm test` - All new tests pass
2. **Check coverage**: `npm run test:coverage` (after installing `@vitest/coverage-v8`)
3. **Target metrics**:
   - Phase 1-2: +30% coverage on services/routes
   - Phase 3: +15% coverage on integration
   - Phase 4: +10% coverage on middleware
   - **Goal**: 70%+ statements, 60%+ branches

**Manual verification:**
- Failing auth tests should pass after fixing subscription backfill issues
- Integration tests run sequentially (no race conditions)
- Database cleanup works in `afterAll()` hooks

**Commands to run:**
```bash
npm test                    # Run all tests
npm run test:watch          # Watch mode during development
npm run test:coverage       # Generate coverage report (requires @vitest/coverage-v8)
npm run test:ui             # Visual test UI
```

## Critical Files

**For immediate testing:**
- `src/server/services/usage-tracker.ts` - Core quota logic (344 lines, 8 functions)
- `src/server/api/routes/subscription.ts` - Subscription API (211 lines, 3 endpoints)
- `src/lib/features.ts` - Feature gate configuration (124 lines, critical tier definitions)
- `tests/integration/auth.test.ts` - Existing pattern to follow (131 lines)
- `src/server/services/__tests__/r2.test.ts` - Unit test pattern example (45 lines)

**For middleware/utility testing:**
- `src/server/api/middleware/quota.ts` - Quota enforcement middleware
- `src/lib/date-utils.ts` - Pure utility functions (115 lines, 9 functions)
- `src/server/api/middleware/auth.ts` - Auth middleware to test

## Notes

- **Install missing dependency first**: `npm install -D @vitest/coverage-v8`
- **Fix failing tests**: Auth tests likely fail because subscription rows aren't created by default anymore (backfill migration needed)
- **Test isolation**: Each test creates unique users with `vitest-${Date.now()}` prefix
- **Database cleanup**: Use `afterAll()` to delete test data
- **Sequential runs**: Config already set (`fileParallelism: false`) to avoid conflicts
- **Mock external APIs**: Don't hit AssemblyAI, OpenAI, Stripe in tests - use mocks/stubs